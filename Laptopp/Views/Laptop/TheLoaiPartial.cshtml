@model IEnumerable<Laptopp.Models.TheLoaiGame>

<ul class="list-group">
    <li class="list-group-item active" style="font-weight:bold; text-align:center">
        Game theo thể loại
    </li>
    @foreach (var item in Model)
    {
        <a href="@Url.Action("GameTheoTheLoai","Laptop", new { iMaTL = item.idLoaiGame})"
           class="list-group-item">@item.TenLoaiGame</a>
    }
</ul>
